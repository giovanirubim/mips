import { Coord, Transform } from '/js/transform-2d.js';
const transform = Transform();
let ctx, sx, sy;
const drawGrid = () => {
	
};
export const setCtx = arg => {
	ctx = arg;
};
export const setSize = (x, y) => {
	sx = x;
	sy = y;
};
export const render = circuit => {
	
};